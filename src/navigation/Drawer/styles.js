import {StyleSheet} from 'react-native';
import {vw} from '../../theme';

const styles = StyleSheet.create({
  iconView: {
    width: vw * 8,
    height: vw * 8,
  },
  icons: {
    width: '100%',
    height: '100%',
    resizeMode: 'contain',
  },
  icon: {
    width: vw * 8,
    height: vw * 8,
  },
});
export default styles;
